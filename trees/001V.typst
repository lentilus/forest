#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Divisor]

Let $n$ be an integer. A #local("001J")[natural number] $d$ is called a
_divisor_  of $n$ if there exists $k in ZZ$ such that $d k = n$
We write $d | n$, say _$d$ divides $n$_.
We denote the set of all divisors by $Dd(n) := {d in NN | d | n}$
We write $d(n)$ for the number of divisors of $n$.
