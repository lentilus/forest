#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Observation]
#title[Products of Solutions of the Pell Equation]

We obtain new solutions by multiplying solutions we have already found.

#proof[
Let $xi$ and $eta$ in $ZZ[sqrt(D)]$ be invertible; then $xi eta$ is also
invertible since $(xi eta)(xi eta)' = xi eta xi' eta' = xi overline(xi)
eta overline(eta) =1$. Therfore, $xi eta$ also corresponds to a solution.
]
