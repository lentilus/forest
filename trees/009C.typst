#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Canonical topology on set with atlas]

There is a canonical topology on every set with a #local("0096")[manifold atlas] given as follows. The open sets are simply the domains of charts.

TODO: Show that this defines a topology.
