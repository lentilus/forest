#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Retraction]

For a #local("005K")[morphism]  $f$ and a left inverse $g$ such that $g f$ is an identity,
we call $g$ a _retraction_ of f.
