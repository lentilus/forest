#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Groesster gemeinsamer Teiler.]

Seien $a$,$b$ ganze Zahlen.
Sind $a,b$ nicht beide $0$,
dann ist die Menge der gemeinsamen #local("001V")[Teiler] $Dd(a,b) := Dd(a) nn Dd(b)$ endlich und nicht leer.
Wir nennen
$
  gcd(a,b) := max Dd(a,b)
$
dann den _groessten gemeinsamen Teiler_ von $a$ und $b$.
