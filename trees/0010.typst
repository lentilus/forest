#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Greatest common Divisor]

Let $a,b$ be integers.
If $a$ and $b$ are not both $0$, then the set of common #local("001V")[divisors]
$Dd(a,b):=Dd(a) nn Dd(b)$
is finite and nonempty.
We then define the _greatest common divisor_ as $max Dd(a,b)$ and denote it by
$gcd(a,b)$.
