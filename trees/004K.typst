#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Conjugate element]

For an element $xi = x + y sqrt(D)$ of $ZZ[sqrt(D)]$ we define its
conjugate by $overline(xi) := x - y sqrt(D)$.
