---
title: True / Unit / Terminal Object
---

```agda
-- open import Cubical.Foundations.Prelude hiding (_∨_)
open import Cubical.Foundations
```

```agda
data ⊤ : Type where
  tt : ⊤

TrueToTrue : ⊤ → ⊤
TrueToTrue = {!!}

TrueToTrue' : ⊤ → ⊤
TrueToTrue' x = {!!}

data ⊥ : Type where

explosion : ⊥ → ⊤
explosion x = {!!}

data ℕ : Type where
  zero : ℕ
  suc : ℕ → ℕ
```
