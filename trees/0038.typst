#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Lemma]
#title[Bezout]
For any #local("001J")[natural numbers] $a, b$. There exist #local("0035")[integers] $x,y$ such that the #local("0010")[gcd] of $a,b$ can we written as $gcd(a,b) = a x + b y$.

#proof[
 Do the #local("000Y")[Euclidean Algorithm] in reverse.
]
