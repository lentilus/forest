#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Affine morphism]

A morphism $pi: X -> Y$ of schemes is called _affine_ if for every affine open subset $V subset Y$, the open subscheme $pi^(-1)(V) subset X$ is an affine scheme.
