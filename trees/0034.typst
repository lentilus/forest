#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Prime Number]

A #local("001J")[natural number] $p$ is called _pime_ if it has exactly two
#local("001V")[divisors]. Hence $d(p)=2$.
