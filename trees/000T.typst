#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Komplex Differenzierbar]

Sei $U sub CC$ offen . $f$ auf $U$ heisst komplex differenzierbar in $z_0 in CC$, falls eine stetige Funktion $Delta$ auf U existert, sodass
$
  AA z in U .. f(z) = f(z_0) + (z-z_0) * Delta(z).
$

