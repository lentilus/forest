#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]
#title[Klassenzahl]

Wir notieren die Anzahl der #local("003L")[primitiven] #local("003M")[Aequivalenzklassen] von 
#local("003I")[Formen] mit #local("003H")[Diskriminante] $D$ mit $h(D)$. Wir nennen $h(D)$ die _Klassenzahl_ von $D$.
