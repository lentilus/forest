#import "/_lib/0.1.1.typ": *
#show: template

#taxon[Definition]

Let $D$ be a natural number and not a square. We denote the set
${x,y in ZZ | x + y sqrt(D)}$ by $ZZ[sqrt(D)]$, read as
_$ZZ$ adjoint with $sqrt(D)$_.
